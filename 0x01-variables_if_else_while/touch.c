#include <stdio.h>
/** main- computing the size of data types
 * Return coorect values
 */
int main(void)
{
printf("Size of a char: %d byte(s)\n", sizeof(char));
printf("Size of an int: %d byte(s)\n", sizeof(int));
return (0);
}

