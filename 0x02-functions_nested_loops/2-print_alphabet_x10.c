#include "main.h"
/**
 * print_alphabet -> prints the lowercase alphabets 10 times
 */
void print_alphabet_x10(void)
{
    int i;
    int j;
    for (i = 0; i < 10; i++)
    {
        for (ch = 'a'; ch <= 'z';)
        {
            _putchar(j);
        }

        _putchar('\n');
    }
}
